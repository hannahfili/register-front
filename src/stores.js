import { writable } from "svelte/store";
export const user_token = writable();
export const user_isAdmin = writable();
export const user_isTeacher = writable();
export const user_isStudent = writable();
export const subject_id = writable();
export const school_class_id = writable();
export const user_id = writable();
